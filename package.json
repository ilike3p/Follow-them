{
  "name": "employee-tracker-database",
  "version": "1.0.0",
  "description": "Employee database built with node.js and MySQL",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "watch": "nodemon server.js",
    "db": "mysql -u root -p < db/schema.sql",
    "seed": "mysql -u root -p < db/seeds.sql",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "employee",
    "tracker"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "console.table": "^0.10.0",
    "express": "^4.16.4",
    "inquirer": "^8.2.5",
    "mysql2": "^3.1.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
